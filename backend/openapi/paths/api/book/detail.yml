get:
  tags:
    - books
  summary:
  description:
  parameters:
  responses:
    200:
      description: Success
      content:
        application/json:
          schema:
            $ref: '../../../components/schemas/book_detail.yml'
